namespace SchoolManagement.Services.Constants;

/// <summary>
/// All raw SQL queries used in auth-domain Dapper reads.
/// </summary>
internal static class AuthQueries
{
    // Add Dapper read queries here as the auth domain grows.
    // Example:
    // public const string GetUserById =
    //     @"SELECT Id, Username, Email, Role, IsActive
    //       FROM Users
    //       WHERE Id = @Id AND IsDeleted = 0";
}
